
dependencies {
    compile "org.photon.common:photon-persist:${photonPersistVersion}"

	compile project(':static-data')
}

apply plugin: 'application'
mainClassName = 'org.photon.staticdata.converter.Converter'
