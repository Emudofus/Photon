apply plugin: 'application'
mainClassName = 'org.photon.login.LoginServer'

dependencies {
    compile "com.typesafe.akka:akka-actor_${scalaVersion}:${akkaVersion}"
    compile project(":protocol")
}
